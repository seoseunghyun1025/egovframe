<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="egovframework.member.service.impl.MemberMapper">
	
	<resultMap id="MemberMap" type="egovframework.member.dto.Member">
    	<result property="memberId" column="MEMBER_ID"/>
    	<result property="email" column="EMAIL"/>
    	<result property="password" column="PASSWORD"/>
    	<result property="name" column="NAME"/>
    	<result property="registerDateTime" column="REGISTER_DATE_TIME"/>
    	<result property="role" column="ROLE" typeHandler="roleHandler"/>
	</resultMap>

    <insert id="regist" parameterType="egovframework.member.dto.Register">
    	INSERT INTO MEMBER(
    		EMAIL,
            PASSWORD,
            NAME,
            ROLE
    	) VALUES(
    		#{email},
    		#{password},
    		#{name},
    		#{role, typeHandler=roleHandler}
    	)
    </insert>
    
    <select id="select" parameterType="String" resultMap="MemberMap">
    	SELECT EMAIL, PASSWORD
    	FROM MEMBER
    	WHERE EMAIL = #{email}
    </select>
    
    <select id="password" parameterType="String" resultMap="MemberMap">
    	SELECT 
    		MEMBER_ID,
    		EMAIL, 
    		PASSWORD, 
    		NAME, 
    		REGISTER_DATE_TIME
    	FROM MEMBER
    	WHERE EMAIL = #{email}
    </select>
    
    <update id="update" parameterType="egovframework.member.dto.Member">
		UPDATE MEMBER
		SET			
			PASSWORD = #{password}
		WHERE EMAIL = #{email}
	</update>

</mapper>